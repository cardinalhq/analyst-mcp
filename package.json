{
  "name": "cardinal-bq-analyst-mcp",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "tsx server.ts",
    "build": "tsc -p tsconfig.json",
    "bundle": "esbuild server.ts --bundle --platform=node --target=node18 --format=esm --outfile=dist/server.bundle.js --external:fsevents",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.18.2",
    "node-fetch": "^3.3.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "esbuild": "^0.25.10",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
